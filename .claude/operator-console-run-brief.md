# Operator Console Redesign — Run Brief (T0)

Working state for the delivery run. Design of record:
`docs/operator-console-ux-design.md`. Binding plan:
`docs/operator-console-delivery-plan.md`. Ledger:
`docs/operator-console-ledger.md`. Errata: `.claude/errata.md` (append-only).

## Preflight (PASSED 2026-09-13, branch tip 3929faf)

- `.venv` built via `uv sync --frozen` (Python 3.12.11, uv 0.8.18). `uv.lock` unchanged.
- `.env` generated by `.venv/bin/python scripts/configure.py`. Admin token 64 hex (≥32 ✓).
- **DB port is 54331, NOT 54329** — 54329 is held by another worktree's Postgres on this
  shared machine. `PHOTO_WALL_DB_PORT=54331` appended to `.env`; `test_local.py` derives
  the test URL from `.env`, so it uses 54331 automatically.
- Postgres up: `docker compose up -d --wait database` (only the `database` service — `up`
  with no arg also builds the `central` image, slow and unneeded for the gate). Container
  `central-mgmt-ux-prompt-e1e1e0-database-1`, healthy, `127.0.0.1:54331`.
- Node pinned: **v20.20.2** at `/Users/matt/.nvm/versions/node/v20.20.2/bin` (npm 10.8.2).
  System default `node` is v16 (too old for Vite) — every npm/vite command MUST prepend
  this to PATH.
- Chromium installed (`playwright install chromium`).
- Baseline: fast+DB gate `1106 passed, 10 skipped`; browser gate (both existing files)
  `6 passed`. Green, unpiped.

## Standard gate commands (every subagent uses these; fresh shells)

Node on PATH (frontend beads):
```
export PATH="/Users/matt/.nvm/versions/node/v20.20.2/bin:$PATH"
```

Fast gate (every bead), run from worktree root:
```
.venv/bin/python -m ruff check .
.venv/bin/lint-imports
.venv/bin/python scripts/check_docs.py
.venv/bin/python scripts/test_local.py -q --tb=short   # exports the DB URL from .env (port 54331)
```

Browser gate (frontend beads), AFTER a green Vite build:
```
export PATH="/Users/matt/.nvm/versions/node/v20.20.2/bin:$PATH"
npm --prefix central/console ci        # first scaffold: npm install
npm --prefix central/console run build # MUST succeed — a broken build is RED (plan §1f)
export PHOTO_WALL_BROWSER_TESTS=1
export PHOTO_WALL_TEST_DATABASE_URL="postgresql://photo_wall:$(grep '^PHOTO_WALL_DB_PASSWORD=' .env | cut -d= -f2)@127.0.0.1:54331/photo_wall"
.venv/bin/python -m pytest tests/browser/<this-bead-file>.py -q --browser chromium --tb=short -rN
.venv/bin/python -m pytest tests/browser -q --browser chromium --tb=short   # full, no regression
```

## Hard rules

- NEVER `uv run` (rewrites uv.lock). Use `.venv/bin/python`. If `uv.lock` drifts,
  `git checkout -- uv.lock` before landing.
- A SKIP / "no tests ran" / "collected 0" for a TARGET test = verification FAILED, not green
  (plan §1e). Assert a positive passed count for the bead's own new test file.
- Mutation probes: pass → red → restore (reverse the edit, never `git checkout`). A probe
  against a skipped test is theater.
- `central/console/dist/` is gitignored (`.gitignore:13 dist/`) → the bundle is BUILT in CI
  and locally, NOT committed. Ensure `central/console/node_modules/` is gitignored too.
- `/`, `central/operator.html`, `central/operator.js`, and the two existing browser test
  files stay GREEN and UNTOUCHED until Bead 17 (cutover).
- One implementer per checkout (this worktree). Serialize beads.

## Confirmed anchors (from recon 2026-09-13)

- `central/app.py:344-352` `index()` → `/` (operator.html) with `Cache-Control: no-store` +
  CSP; `app.py:354-360` `operator_script()` → `/operator.js` (`text/javascript`, no-store,
  no CSP). CSP value (app.py:350): `default-src 'self'; script-src 'self'; style-src 'self'
  'unsafe-inline'; frame-ancestors 'none'`.
- `admin` dependency: `app.py:274-278` (`HTTPBearer(auto_error=False)` + compare_digest;
  routes use `dependencies=[Depends(admin)]`). NO PATCH/DELETE on `/v1/operator/frames/{id}`
  today (only POST collection, PUT/DELETE `.../binding`, POST `.../calibration`; retire is on
  players).
- `FrameCreate` + `oriented_profile` validator live in `central/registry.py:35-49` (NOT
  contracts). Orientation formula: `height_mm != width_mm and ((height_mm > width_mm) !=
  (profile.height_px > profile.width_px))` → raise.
- `Calibration` convex guard: `contracts/models.py:57-63`, `min(cross) <= 1e-6`, clockwise
  screen-space winding; cross[i] over corners[i],[i+1],[i+2].
- `runtime.py:18` `Target = Annotated[str, pattern "^(frame|actuator):..."]` → `visible[].target`
  is the STRING `"frame:<id>"`. `RunView` (runtime.py:171-182) has `phase: str` and
  `participants: frozenset[str]`; NO `.active` (that's on internal `_Run`, runtime.py:231-233,
  = phase in body/outro).
- `installation_models.py`: FrameInventory has surface_id/x_mm/y_mm/width_mm/height_mm/
  generation/calibration/calibration_valid/preview/preview_expires/configuration_revision/
  player_id/output_id; PlayerInventory.is_bound (default False), retired_at, last_seen;
  OutputInventory.observation.connected (OutputReport, contracts/enrollment.py:13-17).
- `001_registry.sql`: frames cols 24-28; outputs compound PK `(player_id,output_id)` line 20;
  `bindings.frame_id TEXT PRIMARY KEY REFERENCES frames(id)` line 37.
- `.github/workflows/checks.yml`: `uv sync --frozen` line 56; browser step lines 89-106.
  Node build stage inserts between line 60 and line 89.
- `pyproject.toml`: `testpaths=["tests"]`; importlinter constrains only contracts/player;
  dev group has pytest-playwright==0.9.0, playwright==1.62.0.

## Harness fact (learned mid-run)
- `Agent(isolation:"worktree")` branches from the REPO BASE (3929faf), NOT the feature-branch
  tip. So an isolated worktree does NOT contain earlier feature beads (e.g. Bead 5). A backend
  bead that must build on a prior feature bead (Bead 6 needs Bead 5) CANNOT be built in an
  isolated worktree — it must be applied in the MAIN worktree (which has the feature tip).
- Bead 5 cherry-picked clean because it was self-contained (backend delta on a console-only tip).
- Bead 6 (B-DELETE) reviewed clean in isolated worktree agent-aa72c395ca65f3bf6 (commit f4859f5),
  but cherry-pick conflicts with Bead 5 (both touch app.py/registry.py/test_operator_frames.py,
  all ADDITIVE). PLAN: apply Bead 6's reviewed delta in MAIN, additively, after Bead 3 lands.
- Remaining beads 7-18 are ALL frontend in central/console/, strictly serial (shared hooks/
  components) → no more cross-track parallelism after M2. Tear down the :54332 Postgres after
  Bead 6 lands.

## Verification-scope optimization (frontend beads)
A frontend-only bead touches ONLY central/console/** + tests/browser/** (confirm with
`git status --porcelain | awk '{print $2}' | grep '\.py$' | grep -v tests/browser/` → empty).
The 12-min `scripts/test_local.py` Python DB suite is INVARIANT across such a bead (it tests
central/*.py etc., unchanged), and it is re-run IN FULL at every milestone coherence review and
in CI. So for frontend-only beads, implementer AND verifier run: ruff + lint-imports + check_docs
+ vite build + the bead's browser test (positive count) + full tests/browser (no-regression) +
the mutation probe — and SKIP `test_local.py`. This saves ~12 min per bead on each side.
BACKEND beads, HIGH-risk beads, milestone coherence, and the cutover STILL run the full gate
incl. test_local.py. If a "frontend" bead unexpectedly touches Python, run the full gate.

## Flaky test (do not chase as regression)
- `tests/browser/test_operator_browser.py::test_browser_same_token_reconnect_fences_delayed_rejection`
  (LEGACY / page) is flaky under full-suite load: it `page.route(**/v1/operator/media, ...)` then asserts
  the request was captured before a click — a route-registration vs click race. Fails ~1-in-N under load,
  passes on isolated re-run. If a full `tests/browser` run shows ONLY this red, re-run it alone to confirm
  environmental, not a regression. Retired at Bead 17 cutover anyway.
