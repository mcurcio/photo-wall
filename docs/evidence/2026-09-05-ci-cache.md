# CI cache optimization checks — 2026-09-05 Pacific

Status: hosted warm image assembly and the earlier enrollment-only VM gate passed;
the newer native-trial/rollback gate remains separately pending. The [CI contract](../module-appliance-ci.md)
owns cache contents, invalidation and trust boundaries.

The [first hosted image build](2026-09-05-github-image.md) spent 741.53 seconds
extracting Ubuntu and 304.84 seconds installing runtime packages. In the next
run, the pinned builder container took 175 seconds. These are the measured
targets; the number of workflow steps alone is not a duration measurement.

The optimization reuses container layers and pristine Ubuntu extraction,
with exact input keys and no configured Player/root or deployment material in
the base cache. Runtime packages, revision-specific Player installation,
signing, finalization and e2e still execute. Build and boot stay on one runner,
avoiding an intermediate 5.9 GB raw disk transfer. Standard checks now run once
per PR update instead of once for its push and again for the PR event.

Executed verification:

- **811 full PostgreSQL-backed tests passed / 10 host-specific skips / 4
  dependency warnings**, in 101.25 seconds. Six skips require GNU tar for cache
  round trips; four are the existing Linux image tools, TFTP and root UID gates.
- **11 cache tests passed in the existing ARM64 Linux builder**, including
  actual GNU tar restore with non-root numeric ownership, modes, hardlinks,
  absolute OS symlinks, xattrs, ACLs and a device node. Fault cases cover stale
  fingerprints, changed archive bytes, traversal, symlink-parent writes,
  configured roots, partial extraction failure, interruption cleanup and
  optional publication failure. No manual Pi image build was performed.
- The revised central container built and imported `central.app`, `media.worker`
  and `player.service` without network access. It retained FastAPI 0.115.12 and
  Photo Wall 0.1.0. An application-only change in a separate disposable context
  reused the dependency-install layer (`CACHED`) and rebuilt only the project
  install in 1.4 seconds. This measures local layer reuse, not remote cache
  transfer or a hosted image speedup.
- Ruff, repository documentation links and both workflows passed validation.
  Workflow validation used checksum-verified official actionlint 1.7.12;
  optional ShellCheck integration was disabled because it was unavailable.

The first optimized hosted run must populate its caches. Subsequent evidence
must record cache hit/miss, restore/save durations, build phase elapsed times,
the exact source/image identity and the e2e result before claiming an overall
speedup. A cache hit does not substitute for artifact or boot qualification.

## Standard media-check cache follow-up

The media Docker build now installs FFmpeg in a source-free stage and copies
the shared application's `/app` tree at the same absolute path. Standard
GitHub checks populate an AMD64 BuildKit cache through `media-test`, then load
the central and worker targets on the same builder. Compose uses their
verified project-specific image names with `--no-build`; the conversion check
runs the existing test image with networking disabled. The hosted behavior
and transfer savings of this additional scope remain pending.

Actual local ARM64 validation of this change:

- The first media package layer completed in **24.6 seconds**. A second build
  from a separate context with only a comment added to `media/__init__.py`
  marked the entire FFmpeg installation layer **CACHED**. Its application
  install completed in **1.3 seconds**. These are local layer measurements,
  not an overall hosted speedup.
- **55 Linux conversion tests passed in 104.47 seconds** in the restructured
  test image, with networking disabled.
- The production worker imported central, worker and Player modules as UID
  10001 using `/app/.venv/bin/python`; it could write its media directory,
  ran FFmpeg 7.1.5 and contained no pytest dependency. The central target
  imported successfully as UID 10001 and contained no FFmpeg binary.
- Compose configuration resolved the exact preloaded central/worker names.
  Ruff, all 53 documentation link sets and actionlint passed.

The previously pushed `6a101a9` standard GitHub run separately passed
**772 tests / 51 explicit skips**, then **55 Linux conversion tests**. That
run predates this media-stage/standard-workflow extension.

## First hosted cache publication

[Run 34015771816](https://github.com/mcurcio/photo-wall/actions/runs/34015771816),
from feature head `6a101a9`, completed signed image assembly on the cold path
in **22 minutes 15 seconds**, from 06:15:01 to 06:37:16 UTC on 2026-09-06.
The base-cache upload then passed in **10 seconds**. GitHub's cache API
confirmed a **1,299,340,290-byte** entry scoped to `refs/pull/2/merge`, created
at 06:37:26.553727 UTC. Its exact key is
`pw-base-v1-Linux-ARM64-43229dbcc962bc9349666ce77a0ce8413067be7aa9a8134e1540ae4c773cdb9c`.

This establishes successful hosted cache population. Boot e2e was still
running when this checkpoint was recorded, and a subsequent verified cache
hit must establish restore time and overall warm-build savings.

The [standard workflow at 901a3a5](https://github.com/mcurcio/photo-wall/actions/runs/34016860582)
then passed on GitHub: **779 tests / 51 explicit skips / 4 warnings in
52.21 seconds**, plus **55 Linux conversion tests in 163.84 seconds**. The
preloaded central/worker images started successfully with `--no-build`.
This verifies the extended standard workflow on an actual hosted runner; a
later run is still needed to measure its cross-run cache restoration.

## First verified hosted warm restore

[Run 34017423186](https://github.com/mcurcio/photo-wall/actions/runs/34017423186)
at feature head `57b3a4d` (actual merge/source
`553cb0d2acaa2144f32b524a57e4fd1c868c7d3c`) restored the exact cache above.
The GitHub cache action took **19 seconds**; internal archive validation and
extraction took **7.716 seconds** and reported `hit: true`. The preceding
cold extraction took **725.636 seconds**, excluding its 8.495-second fetch
and 35.896-second decompression. The builder step fell from **222 seconds**
to **114 seconds**. These are measured phase improvements.

The warm run did **not** complete an image. Its runtime-package phase failed
`tool_timeout` after **1049.966 seconds**; the retained `apt-download.log`
ends during snapshot package acquisition, without a successful download
summary. Assembly stopped after 17 minutes 39 seconds, so this run produced
neither a finalized image nor VM qualification. The cache hit is not an
overall warm-build pass. The package timeout is being investigated separately.

The retained log resolved 309 packages / 159 MB and recorded repeated `Ign:`
download attempts, with no `Err:` or signature/hash failure before termination.
The mitigation sets APT's per-file retries to two and HTTP/HTTPS connection/data
timeouts to 30 seconds inside the disposable root. The outer 900-second command
deadline and signed snapshot verification remain in force. The
[APT configuration contract](https://manpages.ubuntu.com/manpages/jammy/man5/apt.conf.5.html)
and [transport contract](https://manpages.debian.org/bookworm/apt/apt-transport-http.1.en.html)
define these options. This does not establish the remote failure's precise cause
or prove recovery; the next hosted build must measure it. The change to the
fingerprinted builder source invalidates the pristine-base cache once.
The focused package/service suite passed **24 tests / 2 explicit Linux-tooling
skips** after this change.

The [standard workflow at 498f7b8](https://github.com/mcurcio/photo-wall/actions/runs/34018139218)
passed **784 tests / 51 explicit skips / 4 warnings in 47.02 seconds**, then
**55 Linux conversion tests in 153.69 seconds**.

## First completed warm assembly

[Run 34018139184](https://github.com/mcurcio/photo-wall/actions/runs/34018139184)
at feature head `498f7b8` completed signed image assembly in **11 minutes
11 seconds** (07:11:07–07:22:18 UTC), compared with **22 minutes 15 seconds**
for the cold cached run above. Its builder step took **58 seconds**. This
establishes a measured warm assembly improvement of about 50%, although
individual network/package timings can vary. Internal verified cache restoration
took **7.801 seconds**, runtime packages **199.819 seconds**, preparation
**199.967 seconds**, finalization **224.712 seconds**, and generic initramfs
generation **32.2 seconds**. That image predates the RuntimeDirectory fix;
boot e2e failed with the expected missing Player runtime directory. Successful
assembly alone does not qualify its service startup or boot gate.

The next [image run at 1eb16ef](https://github.com/mcurcio/photo-wall/actions/runs/34019308971)
passed the new early service preflight on the actual GitHub ARM64 runner at
07:33:39 UTC, before any container build or image assembly.

The corrected [1eb16ef image workflow](https://github.com/mcurcio/photo-wall/actions/runs/34019308971)
subsequently passed complete generic-VM e2e and published its image. The APT
input change caused a cold cache refresh: assembly was 22m33s, extraction
708.539s and runtime packages 152.826s. Two emulated boots took 9m06s and
9m37s; recovery took 36s. The earlier 11m11s warm measurement remains a
build-phase comparison, not an entire-workflow or two-boot duration. See the
[passing artifact evidence](2026-09-05-github-image.md#first-hosted-exact-image-boot-pass).

The subsequent [d9f656b image run](https://github.com/mcurcio/photo-wall/actions/runs/34020566014)
completed its Ubuntu cache-restoration step in **19s**, then assembly from
08:26:49 to 08:37:02 UTC: **10m13s**, compared with 22m33s for the preceding
cold build (about 55% lower assembly elapsed time). These are GitHub job-step
timestamps; detailed internal phase logs are pending run completion. The
exact-artifact boot step started at 08:37:15 UTC and remains in progress.
This assembly measurement does not predeclare its e2e result.

## Independent standard-check jobs

The [content-correction image run](https://github.com/mcurcio/photo-wall/actions/runs/34023366287)
subsequently passed the enrollment-only VM gate with **9m18s assembly**,
compared with **22m33s cold**, about **59% less assembly time**. This historical
comparison does not qualify the newer native-trial or rollback scenarios.
The [rollback-candidate run](https://github.com/mcurcio/photo-wall/actions/runs/34027456271)
assembled both signed roots in **11m57s** by reusing the configured root for
the candidate; [its evidence](2026-09-06-vm-rollback.md) records the deliberate
cancellation before qualification and the subsequent acceptance correction.

The [standard workflow at d449103](https://github.com/mcurcio/photo-wall/actions/runs/34028634129)
passed **889 tests / 56 explicit skips / four warnings in 60.93s**, followed
by **55 Linux media tests in 163.33s**. Its single job ran from
10:52:47 to 10:57:59 UTC on 2026-09-06 (**5m12s**). The database/application
test step occupied 10:54:01–10:55:03; isolated media conversion then occupied
10:55:03–10:57:48. Image loading was already inexpensive: media-test took
30s, central 3s and worker 3s. Locked host dependency installation took less
than a second at the reported timestamp precision, so an additional dependency
cache is not justified by this measurement.

The workflow now gives media conversion its own independent job. The application
job loads the production worker directly and keeps its existing cache history;
the media job reads shared layers but writes a separate cache scope. Both retain
their existing checks, pinned images/actions and source checkout. Image assembly
and boot remain together to avoid transferring the 5.9 GB raw disk and private
fixture. The split's hosted timing is pending; the 5m12s result above is the
serial baseline, not a claimed parallel speedup.
