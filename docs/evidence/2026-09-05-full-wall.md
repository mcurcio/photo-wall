# Full wall demo evidence — 2026-09-05

Status: one uninterrupted full command passed; complete MVP acceptance remains open. This is actual Immich 2.5.6, Linux conversion, PostgreSQL and HTTP/WebSocket integration with simulated actuation. It establishes no Pi, HDMI, scanout or visible synchronization result. [Harness contract and commands](../module-wall-demo.md).

## Executed run

The retained sanitized record is `/private/tmp/photo-wall-demo-full-final-20260905/evidence.json`. The command began at **2026-09-05 23:55:44.349989 UTC** and finished at **2026-09-06 00:02:05.655156 UTC**. Its timed benchmark phase began at 23:56:03.073142 and took **362.582 seconds**. The scheduled Program spanned 588 seconds within the fixture's ten-minute ceiling; all fault/rejoin checks completed before it ended.

Final checks recorded **two Players, three Outputs, five prepared variants and 485 Readiness-to-Commit checks**, with JPEG and H.264 MP4 presentation. These are the final process reports, not a sum of every intermediate snapshot. Linux preparation identified FFmpeg 7.1.5-0+deb13u1 and Pillow 12.3.0. Independent original/cache checks tied prepared and downloaded bytes to their recorded hashes.

Both new eligible uploads appeared during the same Run. The membership edit preserved six live secured locks; deletion of the selected portrait preserved three at that phase boundary, and its secured derivative was subsequently presented. Permission failure/recovery affected only the demo key. Upstream outage/recovery disconnected only the demo worker's upstream interface.

Central stopped at **00:00:36.557675 UTC**; the latest held lease expired at **00:01:18.948338 UTC**. Fresh reports after expiry showed fallback on all three Outputs, with no media event beyond the allowed expiry tolerance. After central restarted, every Output presented again. Restarting the two-Output Player preserved its identity and cache, advanced authority epoch from 1 to 2 and restored both Outputs. Explicit old-token rejection is covered by separate Player contract tests; this run does not add that claim.

Before acquisition, after acquisition and after recovery, both Players reached central while upstream DNS and numeric TCP access failed. Their runtime lacked central, media and Immich modules. The same Run remained in force through evolution, deletion, faults and rejoin.

## Provenance and review

The core and Player revision was `dda8e98c5c54dc8ca9c007599f8a919eadbd5248`, with no core source drift. The executed harness SHA-256 was `4598de321f03001ec852d265e60c90f9194f3394750e71a82a273e7960e791ea`. The original central and worker image IDs and complete Player wheel inventory remain in the evidence file. The Player wheel SHA-256 was `11987db71bfb853c83b2570097692c1d1c0c0a80b1eaf61ae6c5dc5f06c85575`.

An independent Spark evidence review confirmed the recorded phase claims. Root review strengthened recovery predicates to require every Output and subsequently verified the complete source inventories of both running central and worker images. Both inventories hashed to `00c919371a0a5dde18e95751b5ed503a7bcdd3f6a7613b5d8df8437aa2ffd13d`; both contained the executed harness hash above. The original runtime-provenance file was preserved before adding that audit.

The released harness adds paired exact-image overrides for fresh-host builds and increases the future live-presentation wait from 70 to 120 seconds without changing acceptance predicates. The successful full run used the original 70-second budget. **32 focused harness tests** and scoped Ruff passed after those changes. The new complete source audit also passed against the actual retained services. No second full run is implied.

## Earlier failures retained

Earlier baseline attempts exposed empty-volume ownership reset, an accidental `/media` namespace import caused by placing the runner at filesystem root, and future EXIF timestamps normalized to upload time. The corrected fixture preserves volume ownership, runs beneath `/opt/player` and verifies metadata convergence in a past capture interval.

Earlier full attempts recorded clock-gate losses under CPU contention, an outage assertion that wrongly required a new fallback transition when Outputs were already black, and a connection refusal immediately after restarting central that was incorrectly classified as terminal. Those attempts remain failures. The final uninterrupted run used one CPU per steady service and a quiet interval for timing checks; it retained the strict 100 ms clock bound and all complete-group/byte-integrity predicates.

## Current core revision retry

The revision-selectable harness was run against core and Player revision
`6a101a9d66e5b3cb907bfaebce14361a588eb571`, starting at
2026-09-06 06:25:55 UTC. Its harness SHA-256 was
`b97f99d45acac642b5538c6c7f0e5337d04cde375d572d79743b1f4c87c49d8f`.
The immutable central image was
`sha256:8668e22958c317dc5e643cba51c9f71df9775e9a23a1aa7799abd680dc4fe1f9`;
the worker was
`sha256:dc456b5151db75aca4a0fbed1bf008866439d02285dcf7535c9d12ed66d20a0c`.
Source preflight and runtime inventories passed before execution.

This retry **failed** with `deleted_secured_not_presented`. The baseline
established two Players, three simulated Outputs, real JPEG/video conversion
and 46 Readiness-to-Commit checks. The evolving source and live presentation
phases passed, but the deletion phase did not establish its required secured
presentation within the deadline. Later permission, outage and rejoin phases
did not run. The cause remains under investigation; the earlier historical
pass above is not a pass for this revision. The private synthetic record is
`/private/tmp/photo-wall-demo-full-6a101a9/evidence.json`.

The harness now accepts an explicit complete Git revision, rejects tracked or
untracked core drift before creating state, requires a matching Player
inventory and paired immutable image IDs for a nonhistorical revision, and
retains old marker compatibility. Its focused checks passed **39 tests**.
The initial revision-support change passed **816 PostgreSQL-backed tests /
10 host-specific skips / 4 dependency warnings**, followed by four additional
focused preflight cases. Counts overlap and are not summed.

## Passing revision-bound retry

A subsequent full command **passed** from **2026-09-06 06:38:07.572 to
06:44:08.890 UTC** (361.32 seconds). Core and Player revision:
`901a3a5ee9a6fd4070273b106a687e604a87d2b5`; harness SHA-256:
`cdf268044dd405939ba52711bd5ecfe82c51480fde9a3e56cc296281be88b3a5`.
The matching optimized central image was
`sha256:2b68a374357baf20a159aa3d2e4d817c3e3ddb6cf2620ba992bbd936960f30b9`;
the worker was
`sha256:1613d1b0ce6cbdaa69177567d078a53d7413fe452bcf6277d38be4304432f0f7`.
The source preflight was clean, and both runtime source inventories passed.

Final checks established **two Players, three simulated Outputs, five
prepared variants and 374 Readiness-to-Commit checks**. Both live uploads
appeared in the same Run. Secured deletion, permission loss/recovery, upstream
outage/recovery, fallback beyond every central lease, recovery on all Outputs,
Player restart with retained identity/new authority epoch, and final upstream
DNS/numeric TCP denials passed. Player one's authority epoch changed from
1 to 2; Player two retained epoch 1. Six still-live locks retained their bytes
after deletion. The added diagnostic recorded three future portrait lock
candidates before mutation and persisted deletion invocation/completion
times before waiting for presentation.

The diagnostic change preserves the original success predicates, phase
deadlines and strict 100 ms clock gate; **40 focused harness checks passed**.
It does not claim to fix or establish the cause of the prior missed
presentation. That earlier record showed clock-readiness loss near a skipped
cue but lacked the selected-lock evidence needed for a causal conclusion.
Both attempts remain recorded. This passing command establishes one complete
service integration run, not a reliability rate or physical rendering result.
Private synthetic evidence: `/private/tmp/photo-wall-demo-full-901a3a5/evidence.json`.
