# Exact-VM media gate preparation — 2026-09-06

Status: implementation and bounded service/filesystem preparation checks; **no
full-image native photo or populated-cache rollback qualification yet**. The
[boot contract](../module-appliance-e2e.md#real-media-extension) owns the scenario
and evidence boundaries. No local/manual full Pi image build was performed.

## Actual media-enabled boot services

A disposable fixture `pw-boot-2b01dba01dd43034` used real TLS, DNS, NTP,
PostgreSQL, central and the production worker, with a synthetic tiny signed
rootfs bundle. It borrowed the retained Immich fixture without modifying or
removing that upstream. HTTPS/DNS/NTP/database/scheduler probes passed; the
worker refreshed `vm-photo:1` to status `ok`. Worker media was writable,
connections were read-only, and worker networking was limited to database
and upstream. All new fixture resources and private files were removed;
the sanitized report remains at
`/private/tmp/photo-wall-boot-media-local-20260906/qualification-report.json`.

| Component | Identity |
| --- | --- |
| Central | `sha256:be3636a158d7d21b7d0ac0f41a1e6668a8d6f054c5cb486ae435df50fa13b2a2` |
| Worker | `sha256:b43af1aa72ee0b14f6c0b30217048918cf7b2395833ce906c185ec861a0647ba` |
| Derived boot fixture | `sha256:8bba008d8214732f211ca1aa631b7bd6617d35350bf009c45f35dcf0c2aa84a5` |
| Fixture source inventory | `1736da41acd2ec3c64185ddf5d857830df06ffb900f51ed0816a837588ff9728` |
| Synthetic release | `60a7e422c75b7eb6ac528ca38589ccf41df657d6604eeb2cd6083acceed49009` |
| Synthetic rootfs | `03e86a65cf304d83ab5801fe884a6e282aabe2e5d421472f4f31c0b76b6db19a` |
| Configuration | `b14dd0fd62826b70351192ef7cbee89983f8fde17988084ae5cb779ccf0ae5bc` |

The first attempted initialization exposed that Docker's copy preserved the
host numeric owner of the private connection file. The seed now normalizes
that new owned file to root before the production private-file validator,
then transfers it to worker UID 10001. This does not loosen the loader's
permissions check. Focused fixture tests passed **26 cases** after correction.
This service check did not boot a VM or demonstrate a prepared/drawn photo.

A second disposable fixture, `pw-boot-20e07368c72c517b`, exercised real HTTPS
operator and Player-session APIs with a synthetic durable Player. Frame binding,
calibration, worker refresh and one committed media assignment passed. It used
the retained upstream's nondeleted `orientation-1` asset because that upstream's
portrait was already deleted; the hosted scenario creates its own fresh upstream.
The worker prepared original SHA-256
`fce84adcfcbfc00213cb32300319eccdf429018f45bd8266ba3c753f1e1e79ae`
as variant `0acc38505ddfd2cfdddc35b009cdf63cd95a7995d38cbf86c5064808fd353dfb`.
The sanitized report is
`/private/tmp/photo-wall-boot-media-local-20260906b/operator-qualification-report.json`.
Its derived fixture image was
`sha256:535eaeacac212f40294ad432a635fb2f89352d148e09686612e4193139d3eb82`,
using the same central and worker IDs above. All new resources were removed
and shared upstream state was preserved. This verifies preparation and APIs,
not a native drawing.

That check found the initial helper's portrait physical Frame dimensions were
rejected for a landscape Output with HTTP 422. The corrected helper scales both
physical dimensions from the observed pixel mode. Landscape, portrait and square
requests now pass the production Frame model; independent bounded review found
no material issue. The final focused run passed **51 tests / one dependency
warning**, including real PostgreSQL evidence correlation. Ruff, workflow
validation and all 59 documentation link checks passed.

## Actual read-only cache probe

The existing Linux tooling image
`sha256:fde06a1b8663d43f349c3cdf828cc9202fd2fe388a5c7fff273926e296537f12`
ran the cache helper against tiny synthetic ext4/qcow2 fixtures. Helper SHA-256:
`bf4f441c1b2b74b961dc141ae90b9ab933b2f0f5cfa55abbbee48e543495166e`.
It verified an exact synthetic blob and rejected changed marker bytes,
incorrect size and a symlink blob. Both raw and overlay bytes remained
unchanged. These are filesystem-adapter tests, not a populated appliance cache.

The initial container sandbox failed closed with `guestfs_failed`. Actual
qualification required writable temporary filesystems at `/tmp` and `/var/tmp`,
explicit libguestfs temporary/cache directories, and CHOWN/FOWNER/DAC_OVERRIDE
for supermin's private appliance construction. The integrated container keeps
its root and both input mounts read-only, networking disabled, and all other
capabilities dropped. Disposable containers, volume and staging were removed.

| Tiny fixture | Raw SHA-256 | Overlay SHA-256 |
| --- | --- | --- |
| Verified bytes | `49caf127377ed796486541cdf1882b41a50aa586301d1f2c6982ac27c31dd0ec` | `4dfb0db88def9bd4376bc5b10d1c3db49b1017b4c3fcddaa41023a4204ec2545` |
| Incorrect size | `55de510130d9abd81da81aa1cb24081003bcb8998b38fc3f63ac43de16c15db5` | `076551fc4d09a978bfcf1c2d7aa856749f661a66f257f35ec98e5b098474748e` |
| Symlink blob | `7e27833399743aed0cab4fe8db9f2b3bc6bce017753387a847f0fa479cc4bce9` | `d7d7d36d2cece7b086e6f336b85e4aec37f9a2f52e30c3b806d098b308029e43` |

## Evidence-correlation checks

Real PostgreSQL tests exercised the joined production coordinator records;
their observations were synthetic and do not qualify a renderer. Negative
cases reject other assignments, epochs, revisions, original photos or converted
bytes, missing readiness, invalidated commits, mismatched group/binding,
out-of-interval presentation, and fallback observations. Review caught an
initial missing original-photo binding; the corrected gate requires the
worker's original SHA-256 to match the uploaded portrait, then separately
preserves the converted JPEG SHA-256 across restart/rollback.

The integrated build/boot/media focused run passed **149 tests** with one
dependency warning. The full PostgreSQL-backed suite then passed **1,001 tests /
15 explicit platform or opt-in skips / four warnings** in 110.76s, log
`/private/tmp/photo-wall-native-media-final-regression.log`.

Final evidence review found a false-negative risk: the coordinator renews valid
commit timestamps on later ticks, so its latest row can postdate an already
delivered observation. The correction retains bounded real grant snapshots
during the host's polling window, preserving the strict commit-before-drawing
test instead of inventing historical timestamps. It still requires matching
identity/authority/assignment/group and a valid current commit. **49 focused
tests passed** afterward, including real PostgreSQL grant capture, renewal and
invalidation, with one dependency warning. Hosted image evidence remains pending.

## Hosted media fixture failure at `4ed7a95`

[Run 34031354670](https://github.com/mcurcio/photo-wall/actions/runs/34031354670)
assembled both signed roots in **12m05s** (11:54:36–12:06:41 UTC on 2026-09-06),
then failed during initial media-fixture setup, before any VM boot. The report
recorded only `HarnessError`; all qualification flags are false and cleanup /
original-disk integrity passed. The image was not published. The
[sanitized report archive](https://github.com/mcurcio/photo-wall/actions/runs/34031354670/artifacts/9988960654)
is 1,789 bytes; local report:
`/private/tmp/photo-wall-e2e-34031354670/e2e-report.json`.

| Identity | Value |
| --- | --- |
| Actual PR merge/source | `5e78bf2a5e255490774072b333278236e81316b3` |
| Raw disk SHA-256 | `c5d606dfadf83a1b4dac0ab7f02e8d7414f798d9e43df41208e44199117ba336` |
| Release A | `1069c458e2ad4d84ced2bd0db3e8b8d503ae0d774399c6b752abc6caf46deae9` |
| Rootfs A | `3745daa0258f96d8386c8def0b7628d8af92d278b1c8d45c2a16e85fe948f8cc` |
| Configuration | `4108690a2770ffac1b9b4afdd2be884704e5f1c32d9346d9f3ebf0029bce4c88` |

A bounded Docker reproduction reproduced the builder mismatch in this setup:
a selected `docker-container` builder could not resolve a daemon-local parent
tag (exit 1, source metadata/pull failure); the same tiny Compose build with
`--builder default` succeeded (exit 0). The reproduction created and removed
only its own builder and tags, leaving the original image/configuration intact.
Report: `/private/tmp/pw-builder-repro-rr8j0woe/report.json`.
`FixtureHost.build` now explicitly selects the daemon builder for its loaded
parent. The outer harness also reports `media_fixture_build_failed` for that
phase instead of losing context as a generic exception class. The hosted log
did not preserve its inner error, so the local reproduction establishes the
configuration defect without claiming a recovered hosted traceback.

## Independent cache-gate correction

Integrated strong review found a P2 qualification defect: allowing media
re-download after reboot meant a wiped cache could be repopulated before the
final disk check. The unmodified gate passed a simulated platform that lost its
cache twice and downloaded three times. This did not establish a production
Cache defect; no passing native-image result had depended on it.

After the initial drawing and stopped-disk proof, the corrected harness creates
a persistent media-delivery denial in a separate owned fixture-control volume.
The gateway mounts it read-only and returns a fixed 503 response for media
requests, while control/authority remains available. The worker mounts it as
the checked test writer; its production media garbage collector cannot remove
this separate control state. Exact denial is verified before and after both
later drawings. There is no requirement for zero GET attempts: a cache hit may
still coincide with an unsuccessful network attempt.

Independent review closed P2 after **158 focused tests passed / one database
skip / one dependency warning**. Removing the denial in an in-memory mutation
made both cache-loss regression tests fail; the preserved-cache case passes.
No other P1/P2 was found in the reviewed readiness/commitment/observation/cache
and epoch paths. Earlier selected PostgreSQL review checks passed **59 tests**.
Full-image native rendering, automatic rollback and physical acceptance remain
unqualified.

The root's final fixture/host/gateway run passed **115 tests / one dependency
warning in 3.69s**, including separate control-volume ownership and strict
boolean opt-in. Workflow, Ruff and documentation checks passed. Public fixed
media setup phases now preserve where a future setup failure occurred without
exporting private command output.

## Hosted checkpoint at `c5e6073`

[Standard checks 34032886077](https://github.com/mcurcio/photo-wall/actions/runs/34032886077)
passed **1,004 tests / 56 explicit skips / four warnings** in 101.19s,
plus **55 Linux media tests** in 161.66s. Both jobs started at 12:22:06 UTC
on 2026-09-06; the last finished at 12:25:29 UTC (**3m23s overall**).
These counts overlap the focused and local suites above.

[Image run 34032885905](https://github.com/mcurcio/photo-wall/actions/runs/34032885905)
completed both signed roots in **11m09s** (12:23:58–12:35:07 UTC) and
started exact-artifact e2e at 12:35:16 UTC. At this checkpoint e2e is still
running; assembly is not boot or rendering qualification. The central, worker
and builder steps took 8s, 13s and 56s; Ubuntu cache restoration took 13s.
The [cache evidence](2026-09-05-ci-cache.md) owns earlier cold/warm and parallel
comparisons. Independent performance review found no current cache correctness
defect and no measured reason to split build from boot and transfer the raw disk.

The preceding [238ae59 image run](https://github.com/mcurcio/photo-wall/actions/runs/34031885872)
also assembled both roots (**11m11s**) but failed during media fixture setup
before boot. Its [sanitized report](https://github.com/mcurcio/photo-wall/actions/runs/34031885872/artifacts/9989177064)
recorded `HarnessError`, only the unchanged-disk check, and all qualification
flags false. It predates the explicit daemon-builder correction.

## Bounded native and central bridge

A separate disposable Linux fixture `pw-boot-6009c57c78fbba92` used the retained
native runtime image
`sha256:8bf573f5fd7e4d09e4f8c0e0ad51e70d2ebb6058d3f15d7f7651284d20249e6d`,
offline locked dependency wheels, the central/worker images recorded above and
the retained Immich fixture. It performed no full Pi image build.

The first driver completed its own 180-second timer after **180.096s** and
reported durable identity, healthy state and native capacity. The worker
prepared six blobs. Central subsequently recorded a valid epoch-2 grant with
readiness sequence 153, but **no joined presentation proof** was obtained.
This does not qualify native photo rendering: health does not require a
presentation, the driver's epoch field was hardcoded, and the second driver's
final record was missing. The report does not bind copied source files to an
exact revision. These limits prevent treating the bridge as current-image
acceptance.

Sanitized faults included `observation_capacity`, `stale_commit` and
`connection_failed`. The retained evidence does not establish their precise
cause. The driver's 33ms tick matches production, but its blanket tick-exception
suppression differs from production failure handling. Independent inspection
ran the existing Player-service/executor checks: **98 passed / one skip**.
Those portable checks do not establish an actual native drawing.

All new fixture/driver resources were removed; the shared upstream and network
were preserved. Sanitized report:
`/private/tmp/photo-wall-native-media-20260906b/report.json`.
