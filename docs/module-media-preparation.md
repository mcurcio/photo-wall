# Original-to-variant preparation

Status: implementation contract, 2026-09-05; executable and physical qualification results must be recorded separately. This leaf implements the preparation responsibility in [the media module](module-media.md) and preserves [the central media boundary](requirements.md#central-media-boundary). It neither selects assets nor authorizes execution.

## Interface and ownership

`media/prepare.py` owns `PreparationRecipe`, `PreparationLimits`, `BuildIdentity`, `PreparedMedia`, and `Preparer`. `OriginalAsset` remains in [media/models.py](../media/models.py); `Variant` remains in [contracts/models.py](../contracts/models.py). The central composition owns dependency pins, PostgreSQL, Procrastinate task execution, blob publication/references, and the gateway. This leaf owns only preparation code, its tests, and this document.

`Preparer(ffmpeg="ffmpeg", ffprobe="ffprobe", *, recipe=PreparationRecipe(), limits=PreparationLimits(), clock=SystemClock())` accepts trusted executable locations and an injected Clock. `await prepare(asset, original: Path, destination: Path) -> PreparedMedia` accepts an exact central original revision and a private, absent staging destination. The result contains `path`, an exact `variant`, `original_sha256`, a deterministic `recipe_id`, and `build: BuildIdentity`. It establishes validated bytes, not a Player's playback readiness, capacity, authority, or observed presentation. Expected failures use the existing bounded `MediaError` codes; no raw file names, EXIF, subprocess stderr, or input content enters returned errors.

`await describe_recipe() -> tuple[str, BuildIdentity]` exposes the same bounded inspection and canonical hash calculation used by `prepare`. The worker registers this recipe before selecting or enqueuing jobs and requires `result.recipe_id == lease.recipe_id` before publication. Serialize the build with `build.model_dump(mode="json")`; restore with `BuildIdentity.model_validate`. The recipe digest covers sorted compact JSON containing `recipe` (the recipe model), `threads` (the configured encoder thread count), and `build` (the complete build model). The worker must call this API instead of deriving a second identity. Executable and preparation-source bytes are rehashed before reusing cached inspection and after conversion; a mid-conversion change fails with `preparation_build_changed`. Installed code, Python, linked libraries, and Pillow must remain immutable for a worker process lifetime, with process restart on deployment changes.

The parent worker reserves the original plus the maximum derivative against central aggregate quota before calling preparation. Preparation independently checks the original size, staging bound, and per-output cap. It never downloads, copies an entire source query, retries, edits an original, changes a Frame, or publishes a PostgreSQL ready row. A worker publishes the returned file by its digest using the media repository contract.

## Checks before conversion

Read only regular nonsymlink originals, bound size, and verify SHA-1 against `OriginalAsset` while calculating SHA-256. An original whose declared metadata/file-size/hash no longer agrees fails before encoding. The caller supplies only private staging paths; original filenames and upstream paths are never used to choose destinations. Exclusively create the destination and leave an existing file/symlink untouched. A failed conversion removes its own output; failed cleanup keeps the caller's reservation accounted.

Pillow 12.3.0 is the still metadata/decoder inspector. Run it in a separate process with memory/CPU/file/output/time bounds because native decode cannot be safely cancelled by abandoning a Python thread. Restrict accepted formats to opaque 8-bit JPEG/PNG, verify a complete decode, reject animation/alpha/CMYK, and compare raw dimensions and EXIF orientation 1–8 to the upstream original metadata. A PNG's IHDR bit depth is inspected explicitly because Pillow may expose 16-bit truecolor data through an 8-bit RGB mode. Reject PNG HDR signaling and JPEG gain-map signaling. Check EXIF color-space, PNG gamma/chromaticity, and ICC data rather than inferring color from a filename.

Still color policy accepts untagged ordinary 8-bit sRGB under the declared initial convention. A supplied ICC profile must be valid RGB and map a fixed dense RGB/gray test grid to sRGB through LittleCMS with at most two 8-bit code values of difference. Profile names are not authority. Conflicting gamma/chromaticity or nonidentity/wide-gamut/HDR profiles are rejected; no tone mapping is claimed. The inspector emits bounded normalized facts only. Pillow/LittleCMS metadata behavior is grounded in [Image](https://pillow.readthedocs.io/en/stable/reference/Image.html), [ImageCms](https://pillow.readthedocs.io/en/stable/reference/ImageCms.html), and [tagged PNG parsing](https://github.com/python-pillow/Pillow/blob/12.3.0/src/PIL/PngImagePlugin.py).

FFprobe inspects MP4/MOV containing exactly one H.264 or HEVC video stream. Audio may exist in the input and is discarded; no audio/subtitle/data stream appears in the derivative. Reject invalid or excessive dimensions/duration, unsupported color transfer/primaries, HDR/Dolby Vision signaling, and nonorthogonal display matrices. Normalize supported original sample-aspect ratio and rotation into upright square-pixel output while preserving display aspect ratio. Original pixel quality remains based on the original raster, so resampling never makes a low-quality original eligible. Untagged ordinary HD video uses the declared BT.709 convention; tagged conflicting/unsupported color is rejected. This initial scope assumes static stream color metadata; broader changing-color formats require qualification. [FFprobe](https://ffmpeg.org/ffprobe.html), [FFmpeg options](https://ffmpeg.org/ffmpeg.html).

## Encoding and validation

Disable automatic rotation and apply the validated original transform exactly once. All eight still EXIF transforms follow the mapping in [the media design](module-media.md#original-validation-and-preparation-recipe). Remove residual orientation and inherited private metadata. Keep calibration/cropping/Frame placement in the Renderer; media preparation does not bake them into the original or derivative.

The default JPEG recipe uses a maximum 3,840-pixel long edge, no upscaling, fixed quality, and a single sRGB image. The default video recipe preserves aspect ratio within a 1,920-pixel long edge and 2,073,600 pixels, uses even dimensions/SAR 1, caps 30 fps, and encodes silent H.264 High level 4.2/yuv420p BT.709 MP4 with faststart. Defaults use CRF 23, medium preset, and a two-second GOP. These are conversion settings, not Pi capacity guarantees. The Planner still checks derivative compatibility; a derivative below its required quality floor cannot authorize an unsuitable presentation.

Reprobe and fully decode the result; verify its dimensions, upright orientation, stream count, codec/profile, duration/rate bounds, color tags, and exact byte cap. JPEG has no duration; video has a measured positive duration. Compute the immutable output SHA-256 and fsync before returning its `Variant`. Do not return output on a timeout, partial decode, exceeded limit, or mismatched probe.

Recipe identity covers every conversion setting plus preparation-module/executable SHA-256, executable version/configuration output, the Python/Pillow/LittleCMS/JPEG/zlib inspector versions, and platform identity. A changed build/settings combination has a new recipe identity. Production package/image manifests are still required alongside this fingerprint; a host executable fingerprint does not establish a reproducible Linux appliance build.

## Process and byte bounds

One preparation call has a 180-second wall deadline. Probe/inspector calls have shorter 15-second deadlines. Original bytes are capped at 256 MiB, original dimensions at 16,384 per edge/64 million pixels, video at 300 seconds, JPEG output at 16 MiB, MP4 output at 256 MiB, and original-plus-output staging at 512 MiB by default. Settings are validated finite positive values. Standard output/error are drained concurrently with a 64 KiB bound each; an overproducing tool fails and is killed rather than filling memory or blocking forever.

Use argument arrays without a shell and a fresh process group. A small internal launcher sets resource limits in its own process before running the inspector or executing a trusted FFmpeg/ffprobe binary; avoid `preexec_fn` inside a threaded Python worker. Apply CPU and output-file limits. Linux additionally applies an address-space limit; macOS tests do not claim that Linux memory enforcement was qualified. The deployed worker must also have a read-only runtime filesystem, private staging volume, and container memory/CPU limits. No media-supplied protocol is enabled: input protocols are local files only and allowed demuxers are narrow JPEG/PNG/MOV handlers. Never accept a filter graph or executable argument from source metadata.

Deadline/cancellation/output overflow kills and reaps the whole process group before deleting staging output. A native decoder error is returned as a bounded preparation failure. An explicit clock check supplements asyncio deadlines so fault simulation can detect clock expiry; a stationary fake clock cannot disable the real process deadline.

The preparation failure vocabulary is `preparation_tool_missing`, `preparation_build_changed`, `preparation_timeout`, `preparation_io`, `preparation_limit`, `preparation_invalid`, `unsupported_media`, `unsupported_color`, `metadata_mismatch`, `asset_integrity`, `destination_exists`, `staging_cleanup`, and `clock_invalid`. Unsupported profiles, limits, malformed decodes, and metadata disagreement are incompatible results; I/O, deadline, integrity, build-change, clock, and cleanup errors are unavailable results. The media domain classifies permanent versus retryable results; Procrastinate owns retry scheduling. Cancellation propagates after process reaping and staging cleanup; it is not converted into a successful or empty result.

## Required evidence

Real conversion tests must exercise all EXIF orientations with asymmetric corner markers, original-versus-edited geometry, metadata/hash mismatch, resizing/aspect ratio, silent SDR H.264 output from audio-bearing input, rotated video, HDR/color rejection, corrupt media, output/staging pressure, existing destination preservation, and deadline/cancellation cleanup. Test recipe identity against changed settings/builds. Unit/process-fault evidence and actual FFmpeg conversion evidence must be labeled separately. No such test proves Pi rendering, central delivery, real Immich behavior, or physical continuity.

Available development baseline inspected for this leaf: macOS arm64 Homebrew FFmpeg and ffprobe **8.0.1**, GPL-3.0-or-later build with libx264/libx265, and Pillow **12.3.0** with LittleCMS **2.19**, JPEG **6.2**, and zlib **1.3.1.zlib-ng**. `ffmpeg -version` and `ffmpeg -L` verified the build and license locally. Primary release references are [FFmpeg n8.0.1 options](https://github.com/FFmpeg/FFmpeg/blob/n8.0.1/doc/ffmpeg.texi), [FFmpeg n8.0.1 licensing](https://github.com/FFmpeg/FFmpeg/blob/n8.0.1/LICENSE.md), and [Pillow 12.3.0 license](https://github.com/python-pillow/Pillow/blob/12.3.0/LICENSE). The deployed Linux worker instead uses pinned Trixie FFmpeg **7.1.5-0+deb13u1**, qualified below.

Executed on 2026-09-05: `.venv/bin/python -m pytest -q tests/test_prepare.py tests/test_immich.py` passed **156 tests** (54 preparation and 102 synthetic adapter tests). The subsequently added HEVC MOV/sample-aspect test passed separately (**1 passed, 54 deselected**), making 55 exercised preparation cases. The preparation cases perform real native JPEG/PNG/H.264/HEVC conversion and synthetic process faults using only generated public fixtures; they are separate from the adapter's HTTP MockTransport evidence. Ruff passed for the two preparation Python files, documentation link checking passed, and `git diff --check` was clean.

The measured default local recipe is `7c95356c7d1fdb86faa9c852545db3d3167ea84cb44a7d3e27c1ef831e87d74a`, with preparation source `82222fbbf8b8db8a6a5257e44843c91056129cc0a27d5828b20b39f28b4bf79e`, FFmpeg executable `4046cfa68c9f23e4ca87b332216e8015689091f10749ac56197d19dccb3d029e`, and ffprobe executable `a7bd5c0b4b01f40bc7c0cdee825ef9c848ab991f6fae8403cc59ec59d1e579b5`. This fingerprint describes the inspected host build.

The pinned Linux `media-test` image subsequently passed all 55 preparation tests, including Linux address-space enforcement and rotated video, in 93.50 seconds. Its recipe is `cecba502567286cfe04db85dc23b61c013551e1de3ecdd96e4d1805322dfa76b`; the image records Debian package versions and FFmpeg/ffprobe hashes. The earlier Bookworm FFmpeg 5.1 build failed the unchanged rotated-video check and was superseded. See [the core benchmark](evidence/2026-09-05-core-benchmark.md) for exact image/source identities. Real Immich network isolation has separate adapter evidence; complete real-media wall integration and physical presentation remain pending.
