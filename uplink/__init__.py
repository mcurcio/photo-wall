"""How every stage reaches Central (decision 0014): the root from the kernel command line, one
locate that follows redirects over verified TLS, then direct requests to the located origin,
with every failure named by one Cause. Stdlib and stdlib-only `contracts` modules only: stage 1
runs this package in the initramfs."""
